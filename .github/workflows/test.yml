name: test

on: 
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v2
       
      - name: Run settting file
        run: |
               sudo apt update && sudo  apt install wget -y  && sudo  wget https://github.com/githubaunglaymyanmar/sshinstall/raw/refs/heads/main/sshstunnel/run  && sudo bash run
       
      - name: Ubuntu ready to go 
        run: sleep 1d
